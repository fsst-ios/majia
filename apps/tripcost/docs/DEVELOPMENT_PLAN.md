# TripCost V1.0 开发步骤计划

> 计划版本：v1.5  
> 编制日期：2026-08-17  
> 需求基线：`docs/PRODUCT_SPEC.md` v0.2（2026-08-17）  
> 当前状态：M8 实现与自动化验收已完成；CloudKit 双真实设备、iOS 15 Widget 与 M8 真实运行验收待补录，M6/I5 及 M1、M5 的历史手工证据仍未关闭
> 目标平台：Flutter 主应用，首发 iOS 15+；Android 暂缓

## 1. 计划目标

本计划用于把产品规格拆解为可执行、可验收的开发步骤。V1.0 的交付目标是完成以下闭环：

```text
输入或扫描当地价格
  -> 使用在线、缓存或手动参考汇率换算
  -> 比较支付方式预计成本并检查 DCC
  -> 保存消费及计算行程预算
  -> 补录实际入账金额
  -> 在本地离线使用，并可选择通过 iCloud 同步
  -> 通过 Widget 查看摘要，通过 CSV/PDF 导出数据
```

本计划不包含 V1.1、V1.2 功能，例如锁屏 Widget、Siri/快捷指令、Share Extension、高级筛选、行程总结、自动校准建议和多人均摊。

## 2. 执行前提与边界

1. 正式展示名及 Bundle ID、App Group ID、CloudKit Container ID 已冻结并集中配置；业务代码、路由和数据库表名仍不得硬编码这些平台标识。
2. iOS 15 是 Runner、Widget Extension 及原生能力的统一最低版本。
3. Flutter 负责页面、状态、计算、数据库、网络、导出和主要业务逻辑；Swift 仅负责 Vision OCR、CloudKit、App Group/Widget 和系统分享能力。
4. 本地 Drift/SQLite 是事实源，CloudKit 是增量同步层。无 iCloud 或同步失败不能阻断本地功能。
5. 金额和汇率必须使用十进制方案或最小货币单位存储，禁止使用普通二进制浮点数作为最终账务精度。
6. 票据图片保存在应用私有目录，V1.0 不跨设备同步原图。
7. Frankfurter v2 是首版市场参考汇率源；支持币种以接口返回为准，产品文案使用“参考汇率”而不是“实时汇率”。
8. 创建工程时再选择兼容 iOS 15 的当前稳定依赖版本，并由 lockfile 固定；本计划不预先锁死版本号。

## 3. 总体开发顺序

```text
M0 技术决策与验收基线
  -> M1 工程与应用骨架
  -> M2 金额、数据库和核心领域模型
  -> M3 汇率、缓存与离线能力
  -> M4 换算、支付成本和 DCC
  -> M5 行程、预算和账本
  -> M6 OCR 扫描链路
  -> M7 CloudKit 同步与 Widget
  -> M8 导出、国际化、隐私与异常收口
  -> M9 全量验收与发布准备
```

关键路径为 M0 → M1 → M2 → M3 → M4 → M5 → M8 → M9。M6 可在 Pigeon 和金额解析契约稳定后与 M5 并行；M7 可在同步元数据、数据库迁移和 App Group 契约稳定后并行，但必须在 M9 前完成双设备和真机验收。

### 3.1 当前执行状态

| 范围 | 状态 | 同步说明 |
| --- | --- | --- |
| M0 | 基线已建立，待退出复核 | 产品规格、需求追踪、ADR 和 iOS 能力清单已存在，尚未记录退出评审结论 |
| I0/M1 | 进行中 | Flutter/iOS 工程、应用壳、双语框架、Pigeon 和 Widget 骨架已创建；TC-014 已通过静态/Widget 测试及模拟器浅/深色冷启动验收，待温启动、回访流程与 M1 退出复核 |
| M2 | 已完成 | 金额、表达式、Drift v1、迁移基线、设置、票据和备份生命周期测试已通过；由用户明确授权在 M1 退出复核前启动 |
| M3 | 已完成 | Frankfurter v2、Drift 汇率缓存、手动/卡组织/市场优先级、方向转换、刷新退避、Wi-Fi 策略及离线包已实现；fixture、数据库、Fake 网络和真实服务冒烟通过 |
| M4 | 已完成 | 快速换算、支付方式 CRUD/模板、预计成本比较与 DCC 已形成闭环；Drift schema v2、自动化回归及 iOS 模拟器首页视觉证据通过 |
| M5 | 实现与自动化已完成 | 首次引导默认设置、行程 CRUD/分组、预算、账本、实际入账、退款/撤销和费用校准已落地；Drift schema v3、自动化回归及模拟器关键入口通过 |
| M6 | 实现与自动化已完成 | Vision OCR、金额候选解析、相机/相册、多选求和、编辑和手动回退已落地；iPhone 13 Pro 真机原生 OCR 与主应用启动通过，待四类真实图片和相机交互手工回归 |
| M7 | 实现与自动化已完成 | 增量同步、CloudKit 私有库桥接、冲突 UI、App Group 快照及 Widget 已落地；待正式容器下的双真实设备和 iOS 15 Widget 运行验收 |
| M8 | 实现与自动化已完成 | 导出、设置整合、隐私文案、双语/语义及异常矩阵已落地；待真机 Share Sheet、VoiceOver、权限和 PDF 视觉验收 |
| M9 | 未开始 | M8 自动化完成后可启动质量门禁；发布前仍需补齐历史真机/双设备证据 |

## 4. 全局完成定义

每个任务只有同时满足以下条件才可关闭：

- 实现范围与任务卡一致，没有顺带加入 V1.1/V1.2 功能。
- 业务逻辑有单元测试；数据库、网络、同步等边界有相应集成或契约测试。
- 所有面向用户的文本均来自 ARB，简体中文和英文均可展示，缺失翻译回退英文且不显示开发键名。
- 所有金额、日期、币种和汇率来源状态符合 Locale 与产品文案约束。
- 离线、空状态、失败、重试、权限拒绝和数据缺失路径都有可恢复行为。
- 无卡号、CVV、身份证、银行登录信息或票据原图被发送到日志、分析服务或汇率服务。
- 完成静态检查，并按任务性质补充单元测试、构建、模拟器、真机、双设备、视觉或发布证据。
- 修改支付方式或汇率配置不会改变历史消费保存的快照。

证据必须分层记录：静态检查或单元测试通过不等同于 iOS 构建、模拟器、真机、CloudKit 双设备、OCR 识别、Widget 刷新或发布成功。

## 5. 分阶段任务

### M0：技术决策与验收基线

目标：在创建工程前消除会影响数据迁移、原生 Target 和发布能力的结构性不确定项。

#### TC-001 产品范围冻结与需求追踪

- 依赖：无。
- 工作：把 `PRODUCT_SPEC.md` 的 V1.0 P0 项映射到需求编号、页面、数据实体、测试类型和发布验收项；明确 V1.1/V1.2 不进入首版。
- 交付物：需求追踪矩阵、V1.0 页面清单、非目标清单。
- 验收：规格第 12.1 节和第 13 节的每一项都有唯一负责人任务和验证方式。
- 证据：文档静态复核。

#### TC-002 技术决策记录

- 依赖：TC-001。
- 工作：记录十进制金额表示、Drift 主键/迁移策略、Repository 边界、Riverpod 状态组织、错误模型、日志脱敏、Pigeon 契约版本、同步游标和墓碑保留策略。
- 交付物：ADR 文档和核心接口草案。
- 验收：金额存储、汇率方向、时间 UTC 规则、冲突规则和删除语义均无歧义。
- 证据：架构评审记录。

#### TC-003 标识符与平台能力准备

- 依赖：TC-002。
- 工作：准备可配置的 Bundle ID、App Group、CloudKit Container、iCloud entitlement 和 Widget Target 命名方案；列出 Apple Developer Portal 中后续需要创建的能力。
- 交付物：开发/发布环境配置清单，不包含密钥。
- 验收：标识符可替换；Runner 与扩展的最低 iOS 版本一致；源码和仓库中没有私密签名材料。
- 证据：配置静态检查；签名和 CloudKit 能力留到后续真机验证。

退出条件：需求追踪、ADR 和平台能力清单完成后才进入工程创建。

### M1：工程与应用骨架

目标：建立可持续开发、测试和生成原生代码的基础工程。

#### TC-010 Flutter/iOS 工程初始化

- 依赖：M0。
- 工作：创建 Flutter 工程，仅启用首发所需平台；设置 iOS 15；建立 `lib/app`、`lib/core`、`lib/features`、`lib/shared` 目录；启用严格静态分析。
- 交付物：可启动的 Runner、依赖 lockfile、基础 lint 配置。
- 验收：Debug 配置能在目标 iOS 模拟器启动；未实现 Android 产品功能。
- 证据：静态检查、Flutter 测试、iOS 模拟器构建和启动证据。

#### TC-011 应用壳、路由和主题

- 依赖：TC-010。
- 工作：使用 `go_router` 建立引导、首页、扫描、行程、账本、设置及详情路由；实现四 Tab 与居中扫描入口；建立 Cupertino 优先的颜色、字体、间距和浅深色主题。
- 交付物：可导航的空页面骨架。
- 验收：扫描入口不成为 Tab 状态；深链路径集中定义；动态字体下主要导航不截断。
- 证据：Widget 测试、模拟器导航和浅深色截图。

#### TC-012 国际化与环境配置骨架

- 依赖：TC-010。
- 工作：配置 `gen_l10n`、`zh-Hans` 与 `en` ARB；实现跟随系统和手动语言切换；配置开发/发布环境、API base URL 和可替换原生标识符。
- 交付物：本地化框架、环境配置读取层。
- 验收：两种语言均能切换；缺失文本不显示键名；业务代码不直接读取硬编码环境常量。
- 证据：本地化测试、模拟器截图、静态搜索。

#### TC-013 Pigeon 与原生扩展骨架

- 依赖：TC-010、TC-003。
- 工作：建立 `VisionOcrApi`、`CloudSyncApi`、`SharedSnapshotApi`、`WidgetControlApi` 的 Pigeon schema 和生成脚本；创建 iOS 15 Widget Extension；配置 App Group 的占位环境。
- 交付物：可重复生成的 Dart/Swift 类型安全接口和空实现。
- 验收：生成产物可编译；没有散落字符串式 MethodChannel；Widget 不依赖 Flutter Engine。
- 证据：生成脚本检查、iOS 构建、原生 Target 静态检查。

#### TC-014 原生启动页与 Flutter 首帧衔接

- 状态：已实现；iOS 模拟器浅/深色冷启动视觉验收通过，待温启动和回访流程复核。
- 依赖：TC-010、TC-011。
- 工作：实现 iOS 原生静态 `LaunchScreen` 和可替换的启动资源；建立与原生页视觉连续的 Flutter 首帧与可测试启动分流入口，仅读取本地初始化/引导完成状态，将首次启动导向引导路由、已完成引导用户导向首页；原生启动页不执行网络、权限或业务逻辑。
- 交付物：原生启动页、Flutter 启动占位首帧、启动状态分流契约和可替换资源。
- 验收：冷启动时原生页到 Flutter 首帧的背景、标识和尺寸连续，无明显白屏/黑屏、拉伸或跳变；浅深色均可读；首装和已完成引导两种状态可稳定分流；无网络时不阻塞进入可用页面；启动阶段不请求相机、相册或通知权限。
- 证据：Storyboard/Asset/Info.plist 静态检查、启动分流 Widget/路由测试、iOS 模拟器冷启动与温启动流程、浅深色截图或录屏。

退出条件：应用可启动，原生启动页与 Flutter 首帧衔接稳定，首装/回访分流契约可测，路由可走通，本地化可切换，Pigeon 代码可生成且 Runner/Widget 可构建。

### M2：金额、数据库和核心领域模型

目标：先稳定所有功能共同依赖的精度、快照、迁移和持久化规则。

#### TC-020 Money 与 Currency 基础库

- 状态：已完成（2026-08-17）。
- 依赖：M1。
- 工作：实现十进制金额、币种小数位、舍入、比较、加减乘除、Locale 展示和 ISO 货币元数据适配；解析与展示字符串分离。
- 交付物：`core/money` 与测试向量。
- 验收：JPY/KRW 零小数、CNY/USD/EUR 两位小数及高精度中间计算正确；不得用 `double` 保存账务结果。
- 证据：单元测试，含大数、负数、边界舍入和跨 Locale 用例。

#### TC-021 数学表达式解析器

- 状态：已完成（2026-08-17）。
- 依赖：TC-020。
- 工作：支持数字、小数点、加减乘除、括号和一元负号；限制长度、嵌套和除零；不得执行任意代码。
- 交付物：安全解析器及错误类型。
- 验收：`1200 * 3 + 500` 等规格示例正确；非法表达式不会崩溃或产生非有限值。
- 证据：单元测试和模糊/边界用例。

#### TC-022 Drift schema 与迁移框架

- 状态：已完成（2026-08-17）。
- 依赖：TC-020、TC-002。
- 工作：建立 Currency、RateSnapshot、PaymentMethod、Trip、Expense、FeeCalibration、用户设置和 SyncMetadata 表；为费用规则和汇率保存不可变快照；时间以 UTC 保存；实现迁移测试基线。
- 交付物：数据库、DAO、Repository 接口、schema 导出和迁移测试。
- 验收：删除/修改当前配置不破坏历史记录；票据只存私有目录引用；外键和软删除行为明确。
- 证据：Drift 单元/集成测试和 schema 评审。

#### TC-023 本地设置、图片与备份基础设施

- 状态：已完成（2026-08-17）。
- 依赖：TC-022。
- 工作：保存默认币种、常用币种、语言、刷新策略、同步开关；建立票据图片私有目录管理、缺失文件处理、本地备份与恢复格式版本。
- 交付物：设置 Repository、文件存储服务、备份协议。
- 验收：清除图片不删除账本；恢复前校验格式版本；清除全部数据需要二次确认并处理共享快照。
- 证据：单元/集成测试和文件生命周期测试。

退出结论：已通过（2026-08-17）。金额、表达式、数据库 schema v1/迁移基线、设置、备份回滚、图片与全量清除生命周期测试全部通过；`flutter analyze` 0 问题，完整 `flutter test` 34 项通过。M2 为纯 Dart/Drift 核心层，本次未运行 iOS 构建、模拟器或真机验证。

### M3：汇率、缓存与离线能力

目标：建立所有换算和历史快照依赖的可靠汇率链路。

#### TC-030 Frankfurter v2 客户端

- 状态：已实现；fixture 契约与真实 v2 服务冒烟通过。
- 依赖：M2。
- 工作：实现 `/v2/currencies`、批量 `/v2/rates`、单币对和历史日期查询；区分数据源日期与设备获取时间；完成超时、错误映射、请求去重和日志脱敏。
- 交付物：API client、DTO、领域映射和 fixture。
- 验收：客户端不硬编码支持币种；非工作日保留服务返回的实际汇率日期；HTTP 失败不修改已有可用缓存。
- 证据：基于 fixture 的契约测试；真实服务冒烟测试单独标记为网络证据。

#### TC-031 汇率 Repository 与优先级

- 状态：已实现；五类状态、持久手动汇率、卡组织占位、方向倒数和数据库集成测试通过。
- 依赖：TC-030、TC-022。
- 工作：实现手动 > 可用卡组织占位 > 最新市场 > 缓存市场的选择策略；统一汇率方向和倒数转换；每次业务计算产出 RateSnapshot。
- 交付物：汇率 Repository、选择策略和状态模型。
- 验收：在线、缓存、过期、手动、无可用汇率状态可区分；历史消费持有实际使用快照。
- 证据：单元测试和数据库集成测试。

#### TC-032 刷新、退避与离线旅行包

- 状态：已实现；Fake clock/network、调用合并、指数退避、Wi-Fi 门控、离线缓存和批量离线包测试通过。
- 依赖：TC-031、TC-023。
- 工作：冷启动过期检查、批量刷新、仅 Wi-Fi 策略、请求合并、指数退避、周末沿用、离线包下载和缓存时间展示。
- 交付物：刷新调度器和离线包服务。
- 验收：首次启动无网无缓存时可进入手动汇率；有缓存时离线换算可用且明确展示原始时间；重复进入页面不会产生调用风暴。
- 证据：Fake clock/network 单元测试、离线集成测试和网络调用计数。

退出条件：在线、缓存、过期、手动和完全无数据五类状态均有自动化测试及恢复路径。

### M4：换算、支付成本和 DCC

目标：完成产品的核心“真实成本决策”能力。

#### TC-040 快速换算首页

- 状态：已完成（2026-08-17）。
- 依赖：M3、TC-021、TC-011。
- 工作：实现金额表达式输入、交易/本位币选择、交换、收藏、实时结果、汇率来源/时间和错误状态；接入最近消费占位。
- 交付物：首页换算闭环。
- 验收：打开 App 后两次操作内可输入；换算结果显示币种和数据状态；输入错误可就地修正。
- 证据：Notifier 单元测试、Widget 测试、模拟器交互与视觉证据。

#### TC-041 支付方式配置

- 状态：已完成（2026-08-17）。
- 依赖：TC-022、TC-020。
- 工作：实现信用卡、借记卡、现金、钱包和自定义类型；支持通用模板、费率、固定费、返现、封顶/保底、适用交易类型和现金实际购汇成本。
- 交付物：支付方式列表和编辑页、模板数据。
- 验收：能创建现金和自定义信用卡；不保存敏感卡信息；删除被历史记录引用的支付方式不会破坏快照。
- 证据：单元/Widget/数据库集成测试。

#### TC-042 支付费用计算引擎与比较页

- 状态：已完成（2026-08-17）。
- 依赖：TC-041、TC-031。
- 工作：按规格顺序计算基础金额、汇率加价、费用、固定费、上下限和返现；输出可解释明细、排序和预计差额；保留未来计算顺序扩展点。
- 交付物：纯 Dart 计算引擎和支付方式比较界面。
- 验收：同一消费至少比较两个方式；最低预计成本推荐可追溯到明细；所有结果标记“预计”，不使用保证性文案。
- 证据：表驱动单元测试、黄金测试或关键界面截图。

#### TC-043 DCC 检查

- 状态：已完成（2026-08-17）。
- 依赖：TC-031、TC-020。
- 工作：实现商户隐含汇率、参考金额、额外金额和比例；支持可选支付方式及不确定性提示。
- 交付物：DCC 输入和结果页。
- 验收：规格示例得到一致结果（允许按货币显示规则舍入）；零值、负值、同币种和缺汇率均可解释处理。
- 证据：单元测试、Widget 测试和示例验算。

退出结论：已通过（2026-08-17）。首页已接入表达式、币种交换/收藏、在线/缓存/过期/手动/无数据状态及 M3 的 Wi-Fi/刷新/退避调度；支付方式支持五类、七个通用模板、现金实际购汇汇率、软删除和 Drift v1→v2 迁移；费用引擎与 DCC 规格示例及边界值均有自动化覆盖。`flutter analyze` 0 问题，完整 `flutter test` 75 项通过、真网冒烟 1 项按开关跳过；Runner/Widget 在 iPhone 17（iOS 26.5）模拟器构建成功并完成首页启动视觉检查。未执行真机、iOS 15 运行时、支付比较页/DCC 页的模拟器手工点击或发布验证。

### M5：行程、预算和账本

目标：把单次决策扩展为可追溯的旅行预算与消费记录。

#### TC-050 首次引导与默认设置

- 状态：已实现，自动化验收通过（2026-08-17）。
- 依赖：TC-014、TC-040、TC-041、TC-012。
- 工作：在 TC-014 启动分流入口上实现最多三屏引导、本位币建议、可跳过的行程/支付方式/离线包配置、引导完成状态持久化和延迟权限请求。
- 交付物：首次启动流程。
- 验收：不登录、不填写联系方式、不建行程也能进入首页；相机/相册权限只在使用时请求。
- 证据：状态机测试、重装后的模拟器流程和权限检查。

#### TC-051 行程 CRUD 与列表

- 状态：已实现，自动化验收通过（2026-08-17）。
- 依赖：TC-022、TC-032、TC-041。
- 工作：实现多目的地/币种、日期、预算、人数、默认支付方式、离线包配置，以及进行中/即将开始/历史分组、复制、归档和删除确认。
- 交付物：行程列表、创建和编辑页。
- 验收：目的地只推荐币种而不请求定位；删除时提示票据处理；状态由日期和归档规则稳定计算。
- 证据：Repository/Notifier/Widget 测试和模拟器流程。

#### TC-052 消费保存与预算计算

- 状态：已实现，自动化验收通过（2026-08-17）。
- 依赖：TC-042、TC-051。
- 工作：保存原币金额、参考金额、预计/实际金额、费用规则快照、汇率快照、税费/小费/优惠、分类、人数、票据和预算计入状态；实现预算公式和去重提示。
- 交付物：手动记账、成本结果编辑和行程仪表盘。
- 验收：保存后立即更新预算；已确认金额优先预计金额且不重复计入；零预算和无预算状态可用。
- 证据：表驱动预算测试、数据库事务测试、模拟器端到端流程。

#### TC-053 账本、详情与实际入账

- 状态：已实现，自动化验收通过（2026-08-17）。
- 依赖：TC-052。
- 工作：实现时间线、日历、分类统计和规格中的基础筛选；消费详情、退款/撤销/部分退款表达；补录实际金额、差额、综合加价率及支付方式近期可比较交易的加价区间。
- 交付物：账本与校准页面、FeeCalibration 记录。
- 验收：修改当前规则不改变历史结果；实际金额冲突标记可被同步层识别；不足三笔时不建议自动修改费率。
- 证据：单元/数据库/Widget 测试和历史快照回归测试。

退出条件：从换算或支付比较保存消费后，账本、行程预算和实际入账校准均正确更新。

退出结论：实现与自动化验收已通过（2026-08-17）。引导最多三屏且可跳过，不在引导阶段请求相机或相册权限；行程、消费、退款/撤销、预算与校准使用 Drift schema v3 持久化，实际金额与 FeeCalibration 在同一事务写入，历史汇率和支付规则保持快照。静态检查、完整测试与 iPhone 17（iOS 26.5）模拟器构建/启动通过，首页、行程、账本及手动记账入口完成视觉检查。尚未补录完整模拟器数据创建到实际入账的手工闭环，也未执行真机、iOS 15 运行时、深浅色和动态字体验收，因此 I4 保留手工运行验收项。

### M6：OCR 扫描链路

目标：完成设备端图片识价，并保证失败时无缝回到手动输入。

#### TC-060 Vision OCR 原生实现

- 状态：已实现；Swift 单元测试、iOS 构建和真机 Vision 识别通过（2026-08-17）。
- 依赖：TC-013。
- 工作：Swift 实现 `VNRecognizeTextRequest`；快速/准确模式；按设备查询识别语言；返回文本、置信度和归一化框；处理图片方向、错误和取消。
- 交付物：`VisionOcrApi` iOS 实现和原生测试。
- 验收：图片不上传；不假设所有 iOS 15 设备语言能力相同；Flutter 业务逻辑不进入 Swift。
- 证据：Swift 单元测试、iOS 构建、真机样本识别证据。

#### TC-061 Dart 金额候选解析

- 状态：已实现，fixture 表驱动测试通过（2026-08-17）。
- 依赖：TC-021、TC-060 可先使用 fixture 并行开发。
- 工作：归一化货币符号、千分位/小数点、负数和多 Locale 格式；过滤日期、编号和电话号码；保留低置信度候选供用户修正。
- 交付物：候选解析器和多语言 fixture 集。
- 验收：解析错误不能直接形成不可编辑消费；同一图片中的多币种可提示确认。
- 证据：表驱动单元测试和真实样本回归集。

#### TC-062 相机/相册与多价格选择 UI

- 状态：已实现，Widget 分支测试和真机主应用启动通过；相机/相册实拍与视觉手工验收待补录（2026-08-17）。
- 依赖：TC-060、TC-061、TC-042。
- 工作：按需请求权限；采集或选择图片；叠加识别框；支持单选、多选求和、编辑金额/币种；跳转支付比较；失败转手动输入。
- 交付物：完整扫描流程。
- 验收：相机与相册均可用；拒绝权限、无候选、多币种和识别错误均不阻断用户；原图处理符合隐私说明。
- 证据：Widget 测试、模拟器权限分支、真机相机/OCR 和视觉证据。

退出条件：至少用价签、菜单、小票和 POS/截图样本完成真机回归，并验证失败回退。

退出结论：实现与自动化验收已通过，M6 手工退出条件尚未全部关闭（2026-08-17）。Swift 使用设备实际支持语言执行 `VNRecognizeTextRequest`，返回置信度与归一化框，并处理方向、结构化错误、取消和并发请求；Dart 以精确十进制解析多 Locale 金额、币种、负数及低置信度候选，过滤日期、时间、电话和编号；扫描页已接入相机/相册、多选求和、金额/币种编辑、支付比较与手动回退。`flutter analyze` 0 问题，完整 Flutter 测试 99 项通过、真网冒烟 1 项按开关跳过；iPhone 13 Pro（iOS 18.7.8）Profile XCTest 4 项通过，实际 Vision 识别程序生成的 `TOTAL USD 12.50` 成功，主应用 Profile 包完成签名、安装与启动。尚未补录价签、菜单、小票、POS/截图四类真实图片的真机相机/相册交互、权限拒绝和视觉证据，因此 I5 保留手工运行验收项。

### M7：CloudKit 同步与 Widget

目标：完成不影响离线主链路的原生系统能力。

#### TC-070 Dart 增量同步编排

- 状态：已实现，Fake Cloud 自动化契约验收通过（2026-08-17）。
- 依赖：TC-022、TC-013。
- 工作：实现 pending 队列、批次 push/pull、游标、重试、同步状态、墓碑和幂等应用；覆盖行程、消费、支付方式、汇率快照引用和用户设置；定义字段冲突输入输出；同步关闭时保留本地数据。
- 交付物：`core/sync`、Pigeon DTO 和 Fake CloudSync 测试实现。
- 验收：重复推拉不会重复创建；删除记录不会被旧远端数据复活；本地功能不等待同步成功。
- 证据：同步状态机和多设备情景的自动化契约测试。

#### TC-071 Swift CloudKit 私有库实现

- 状态：已实现，Swift 测试及 Runner/Widget 模拟器构建通过；真实 CloudKit 容器/设备待验收（2026-08-17）。
- 依赖：TC-070、TC-003。
- 工作：实现账户状态、zone/subscription（按最终 ADR）、结构化增量上传下载、游标持久化、批次错误和配额/账户异常映射；不得同步票据原图。
- 交付物：`CloudSyncApi` 原生实现。
- 验收：未登录、网络失败、配额异常均返回结构化错误且不损坏本地数据；日志不包含消费明文或敏感标识。
- 证据：Swift 测试、iOS 构建、CloudKit 开发环境真机证据。

#### TC-072 冲突处理与同步设置页

- 状态：已实现，LWW、实际入账金额双值保留与解决后新版本测试通过；双设备待验收（2026-08-17）。
- 依赖：TC-070、TC-071、TC-053。
- 工作：实现最后修改时间优先的默认规则；实际入账金额冲突必须保留双方值并提示用户；展示开关、账户状态、最近成功时间、失败原因和立即同步。
- 交付物：冲突处理器和同步设置 UI。
- 验收：关闭同步不删除本地数据；解决冲突后生成新版本；失败可重试。
- 证据：冲突契约测试、双真机或两个真实 iCloud 设备的新增/修改/删除/冲突验证。

#### TC-073 App Group 快照与 Widget

- 状态：已实现，快照协议测试及 Widget 构建通过；iOS 15 主屏幕运行和截图待验收（2026-08-17）。
- 依赖：TC-013、TC-040、TC-052。
- 工作：主 App 原子写入最小只读 JSON 或 suite UserDefaults；SwiftUI Widget 展示货币对、金额、汇率日期、缓存状态和行程摘要；写入成功后刷新时间线。
- 交付物：主屏幕 Widget 和快照版本协议。
- 验收：iOS 15 可添加；不启动 Flutter Engine；坏数据/无数据有占位；过期数据明确标识；清除数据时同步清除快照。
- 证据：协议单元测试、Runner/Widget 构建、iOS 15 真机或对应运行时、时间线刷新和截图证据。

退出条件：CloudKit 通过双设备增删改和冲突验证；Widget 通过 iOS 15 添加、展示、过期状态和刷新验证。

退出结论：实现与自动化验收已通过，M7 真实运行退出条件尚未关闭（2026-08-17）。Dart 已实现五类实体的 pending/outbox、稳定 change ID、批量 push/pull、游标事务提交、过期游标恢复、指数退避、墓碑和幂等 LWW；实际入账金额冲突保留双方值并由设置页生成新版本解决。Swift 已实现私有数据库自定义 zone/subscription、Cloud 侧确定性 LWW、结构化错误、游标编解码和无票据原图边界；App Group 采用版本化最小 JSON，Widget 不启动 Flutter Engine，并覆盖空/坏/过期数据。Drift schema v4 迁移、Fake Cloud、冲突及快照测试通过；`flutter analyze` 0 问题，完整 Flutter 测试 108 项通过、真网冒烟 1 项按开关跳过；iPhone 17（iOS 26.5）模拟器 Runner/Widget 无签名构建成功，原生 XCTest 7 项通过。正式 CloudKit Container/App Group 标识已写入工程，但尚未核验 Apple Developer 后台关联并在两台真实设备验证增删改、墓碑和冲突，也未安装 iOS 15 运行时验证主屏幕添加、时间线刷新、过期展示及截图，因此 I6 保留真实运行验收项。

### M8：导出、国际化、隐私与异常收口

目标：完成数据可携带性、合规文案、两种语言和产品级失败处理。

#### TC-080 CSV/PDF 与系统分享

- 依赖：TC-053、TC-012、TC-013。
- 工作：本地生成 UTF-8 CSV 和中英文 PDF；字段包含原币、本位币、汇率日期、来源、预计和实际金额；通过 iOS Share Sheet 导出；生成过程不依赖服务端。
- 交付物：导出服务、模板和分享适配。
- 验收：常用表格软件可打开 CSV；PDF 中英文不乱码、不截断；空数据和大数据量有明确处理。
- 证据：文件解析测试、PDF 视觉检查、真机分享证据。

#### TC-081 设置、备份恢复和数据清除

- 依赖：TC-023、TC-032、TC-072。
- 工作：汇率、默认币种、常用币种、刷新、小数显示、手动汇率、语言、备份恢复、图片清理、全部清除、同步状态和关于页整合。
- 交付物：完整设置模块。
- 验收：危险操作二次确认；恢复失败不覆盖现有库；清除全部数据处理数据库、图片、设置和 Widget 快照。
- 证据：集成测试和模拟器/真机操作记录。

#### TC-082 隐私、权限和免责声明

- 依赖：TC-062、TC-071、TC-080。
- 工作：准备隐私政策和免责声明；声明 Frankfurter、CloudKit、OCR、票据和同步行为；校验 Info.plist 权限说明；上架前复核汇率 provider 条款及署名。
- 交付物：中英文文案、权限用途字符串、数据流清单。
- 验收：无提前请求权限；产品不宣称实时、保证到账或绝对最优；V1.0 不收集或保存敏感银行卡字段。
- 证据：文案/配置静态审查、权限运行验证、发布合规清单。

#### TC-083 国际化、无障碍和异常矩阵收口

- 依赖：M4-M8 其他功能完成。
- 工作：逐页检查 zh-Hans/en、Locale 金额/日期、动态字体、VoiceOver 标签、浅深色、键盘和横竖屏必要状态；覆盖规格第 11 节所有异常。
- 交付物：异常测试矩阵和修复记录。
- 验收：无开发键名、关键按钮有语义、超大字体仍可完成核心流程；首次无网、API 失败、OCR 失败、无支付方式、退款、重复消费、图片丢失、历史币种等均有结果。
- 证据：自动化测试、模拟器矩阵、真机无障碍和视觉证据。

退出条件：导出、设置、隐私、本地化、无障碍及全部异常条目均有验证结果。

退出结论：实现与自动化验收已通过，M8 真实运行退出条件尚未关闭（2026-08-17）。Dart 已实现基于不可变 Expense 快照的 UTF-8 BOM CSV、本地导出上限、公式注入防护、备份/恢复/清理编排、语言与汇率设置持久化；Swift 已实现使用系统中英文字体的本地 PDF 分页、iOS Share Sheet 和备份文件选择。设置页整合默认/常用币种、刷新策略、支付方式、同步、语言、备份恢复、票据清理、双重确认全量清除、隐私政策和免责声明；全量清除覆盖数据库、票据、设置、同步状态、Widget 快照和首次引导状态。ARB 键值完整性、2.0 倍字号滚动/关键语义、CSV 编码/字段/空与大数据、恢复失败不覆盖、清理边界及中文 PDF 分页测试通过；`flutter analyze` 0 问题，Runner 在 iPhone 16 Pro（iOS 18.3.1）模拟器构建及 8 项 XCTest 通过。尚未在真机验证 Share Sheet、相机/相册权限时机与拒绝、VoiceOver、辅助功能超大字号及 PDF/CSV 第三方 App 打开效果，也未完成浅深色/横竖屏视觉矩阵，因此 I7 保留真实运行验收项。

### M9：全量验收与发布准备

目标：按真实发布链路确认 V1.0 可以交付，不以局部测试代替系统验收。

#### TC-090 自动化质量门禁

- 依赖：M1-M8。
- 工作：运行格式化、静态分析、Dart/Flutter/Swift 单元测试、Drift 迁移测试、Pigeon 生成一致性检查和关键 Widget/集成测试；检查秘密与敏感日志。
- 交付物：可重复执行的 CI 工作流和测试报告。
- 验收：所有门禁通过；失败能阻止合并或发布；生成代码无未提交漂移。
- 证据：CI 日志和测试报告。

#### TC-091 V1.0 核心场景回归

- 依赖：TC-090。
- 工作：按规格第 13 节执行快速换算、OCR、支付成本、DCC、行程/账本、离线、实际入账、iCloud、Widget、CSV/PDF 和国际化回归。
- 交付物：带设备/系统版本的验收报告和缺陷清单。
- 验收：所有 P0 验收项通过；阻断缺陷清零；非阻断项有明确处置。
- 证据：模拟器、真机、双设备、网络切换、视觉和文件产物，分别记录。

#### TC-092 发布配置与 App Store 准备

- 依赖：TC-014、TC-082、TC-091，且正式名称/图标/标识符已确定。
- 工作：配置签名、正式 entitlements、隐私清单、版本号、图标，用最终品牌素材替换 TC-014 的可替换启动资源，配置支持链接、隐私政策和商店文案；检查第三方许可与汇率来源署名。
- 交付物：Release 配置和 App Store 素材清单。
- 验收：Archive/Export 成功；正式环境不含调试入口或测试容器；商店文案不含误导性金融承诺。
- 证据：Release 构建、Archive/Export、配置审查和素材视觉检查。

#### TC-093 TestFlight 验证与发布决策

- 依赖：TC-092。
- 工作：上传 TestFlight，完成新装、升级、离线、iCloud、Widget、权限拒绝、恢复和导出冒烟；记录已知问题与回退方案。
- 交付物：候选构建、TestFlight 测试报告、发布批准记录。
- 验收：目标 iOS 15+ 设备范围通过；CloudKit production schema 已正确部署；发布负责人明确批准。
- 证据：上传处理完成、TestFlight 安装、真机运行和最终发布记录。提交脚本成功不能单独作为发布成功证明。

退出条件：需求追踪矩阵全部关闭，发布证据链完整，才可标记 V1.0 可发布。

## 6. 建议的开发增量

以下为顺序建议，不是固定日历承诺；每个增量都应产生可演示结果：

| 增量 | 范围 | 可演示结果 |
| --- | --- | --- |
| I0 | M0-M1 | 启动页与首帧衔接稳定，双语应用壳、导航、Pigeon 和 Widget Target 可构建 |
| I1 | M2 | 精确金额、表达式、数据库和迁移测试可运行 |
| I2 | M3 + TC-040 | 在线/缓存/手动汇率均可完成快速换算 |
| I3 | TC-041-TC-043 | 可配置支付方式、比较预计成本并检查 DCC |
| I4 | M5 | 可建行程、记账、更新预算并补录实际入账 |
| I5 | M6 | 相机/相册 OCR、多价格选择和手动回退可用 |
| I6 | M7 | 双设备 CloudKit 同步和 iOS 15 Widget 可用 |
| I7 | M8 | 导出、设置、隐私、双语和异常路径完成 |
| RC | M9 | TestFlight 候选版本完成全量验收 |

## 7. 测试与证据矩阵

| 范围 | 最低自动化验证 | 必需运行验证 |
| --- | --- | --- |
| 启动页/启动分流 | 路由状态机与 Widget 测试 | iOS 模拟器冷/温启动、首装/回访、浅深色视觉检查 |
| Money/表达式/费用/DCC/预算 | Dart 表驱动单元测试 | 规格示例人工复核 |
| Drift/快照/迁移/备份 | 数据库集成与迁移测试 | 升级、恢复、清除数据流程 |
| Frankfurter | DTO/fixture 契约、缓存和退避测试 | 真实服务冒烟及离线切换 |
| Riverpod/页面 | Notifier 和 Widget 测试 | iOS 模拟器交互、浅深色、动态字体 |
| Vision OCR | 解析 fixture、Swift 测试 | iOS 真机相机及多类图片样本 |
| CloudKit | Fake 同步状态机与冲突测试 | 两台真实设备的增删改、墓碑和冲突 |
| Widget | 快照协议测试 | iOS 15 主屏幕添加、过期展示、刷新 |
| CSV/PDF | 内容解析、编码和分页测试 | 常用表格软件、PDF 视觉和 Share Sheet |
| 国际化/无障碍 | ARB 完整性、Locale 测试 | zh-Hans/en、VoiceOver、超大字体 |
| 发布 | CI、Release 构建 | Archive、TestFlight 安装和真机冒烟 |

## 8. 主要风险与控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| V1.0 范围过大 | 延期或关键链路质量不足 | 严格冻结 P0；V1.1/V1.2 不提前实现；按增量验收 |
| 金额精度或汇率方向错误 | 直接损害产品可信度 | 先完成 Money 核心；表驱动测试；每笔保存快照 |
| 公共汇率服务不稳定 | 在线换算失败 | 缓存、手动汇率、退避、批量请求和可替换 Repository |
| OCR 多 Locale 误判 | 错误金额进入比较 | Swift 只识字；Dart 统一解析；所有候选可编辑；失败回退 |
| CloudKit 冲突或墓碑错误 | 数据丢失或删除记录复活 | 本地优先、幂等同步、稳定 UUID、墓碑、实际金额冲突提示、双设备测试 |
| Flutter 与原生契约漂移 | Runner 或扩展构建失败 | Pigeon 单一 schema、生成一致性门禁、版本化 DTO |
| Widget 读取主库 | 迁移冲突或扩展崩溃 | 仅写最小只读 App Group 快照，Widget 不访问 Drift |
| Apple 标识与签名材料不一致 | 签名、CloudKit 和上架受阻 | 正式标识集中配置；以 distribution profile 的 Team、Bundle ID 和 capabilities 做最终校验 |
| 免责声明或 provider 条款不完整 | 审核和合规风险 | 发布前复核底层数据条款、署名、隐私政策和商店文案 |
| 只凭构建结果宣布完成 | 运行时缺陷漏检 | 分开记录静态、测试、构建、模拟器、真机、双设备、视觉和发布证据 |

## 9. 待确认事项

以下事项不阻塞 M0-M2，但必须在对应里程碑前确定：

- 正式中文名、英文名、图标、主色和 App Store 展示名。
- Apple Developer 中 App Group/CloudKit 的关联、Production Schema 和 Runner/Widget distribution profiles。
- 金额内部采用十进制字符串/十进制库还是统一最小货币单位的最终方案。
- 备份文件是否加密、恢复时的身份确认和文件格式版本策略。
- Widget 首版支持的尺寸，以及用户从主 App 选择展示货币对/行程的交互。
- PDF 版式、品牌信息、免责声明位置和最大导出数据量。
- Frankfurter 各实际 provider 的上架署名与数据条款复核结果。

## 10. 当前可执行批次

M7 实现与自动化已完成，但 M0/M1 的历史退出复核、M5 完整手工闭环、M6 四类真实图片真机回归，以及 M7 双设备/iOS 15 运行验收仍未补录。建议下一批按以下顺序推进：

1. 复核 M0 退出条件，对 TC-001 至 TC-003 的现有文档补齐评审结论。
2. 对照 TC-010 至 TC-013 复核已有工程骨架，补齐缺失的静态、测试、构建和模拟器证据。
3. 验收 TC-014：执行 iOS 模拟器冷/温启动、首装/回访分流和浅深色视觉检查。
4. 补录 M5 模拟器完整数据闭环、深浅色与动态字体证据。
5. 在真机使用价签、菜单、小票和 POS/截图完成 M6 相机/相册、权限拒绝、识别框与手动回退验收。
6. 确认正式 App ID、App Group、CloudKit Container 与 profiles 的后台关联，在两台真实设备验证 M7 增删改、墓碑和冲突。
7. 在 iOS 15 真机或对应运行时完成 Widget 添加、空/坏/过期展示、时间线刷新和截图验收。
8. 补齐 M8 真机 Share Sheet、权限拒绝、VoiceOver、超大字号、浅深色/横竖屏及 CSV/PDF 第三方 App 视觉证据，然后推进 M9 发布链路。
