## 0.1.1

* Replace the `MBProgressHUD` dependency with a UIKit loading overlay for
  `showLoading` and `hideLoading`, so consuming apps no longer embed that SDK.

## 0.1.0

* First reusable STMini Flutter component release.
* Vendor the iOS STMini runtime, verified package installer, generic H5 bridge,
  reusable external WebView entry point and App Store privacy manifest.
